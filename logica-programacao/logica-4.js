const numbers = [90, 10 , 30];
let sum = 0; //utilizar um contador quando se tratar de soma, somar o contador mais a const[i]

for (let i = 0; i < numbers.length; i++){
    sum = sum + numbers[i];
        
}
console.log("O resultado é: " + sum)